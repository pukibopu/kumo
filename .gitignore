# build output
build/
cmake-build-*/
.cache/
compile_commands.json
golden_actual_*.png

# OS noise
.DS_Store

# secrets and machine-local config
.env
kumo.config.json

# runtime-generated shaders live locally until reviewed and promoted
shaders/generated/

# generated at runtime
imgui.ini

# fetched asset library (tools/fetch_assets.sh); only the two demo assets
# actually shipped in the repo are exempted
assets/textures/*
assets/models/*
!assets/models/DamagedHelmet.glb
assets/env/*
!assets/env/studio_small_09_2k.hdr

# generated by `viewer --thumbnails` / `viewer --index` (MA/MR milestones)
assets/.thumbnails/
assets/index.json
assets/index_embeddings.bin
